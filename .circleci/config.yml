version: 2.1

orbs:
  welcome-orb: circleci/welcome-orb@volatile

workflows:
  welcome:
    jobs:
      - build:
          context:
            - dockerhub-credentials
      - welcome/run:
          context:
            - dockerhub-credentials

jobs:
  build:
    docker:
      - image: circleci/buildpack-deps:latest
    steps:
      - run: echo 'ok'
